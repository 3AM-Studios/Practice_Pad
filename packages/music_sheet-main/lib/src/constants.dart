class Constants {
  static const staffSpace = 25.0;
  static const finalBarlinePadding = 250.0;
  static const measureLineSpacing = 138.0; // Vertical spacing between staff lines (when isNewLine = true)
  static const firstMeasureLineSpacing = 0.5; // Spacing between 1st and 2nd measure lines (0.9 * staffSpace)
}
